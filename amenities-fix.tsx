/**
 * Placeholder file to prevent build errors.
 *
 * The previous content contained a JSX snippet intended for documentation/reference
 * and was not a valid standalone component. Keeping this file as an empty module
 * ensures the TypeScript compiler ignores it during builds.
 */
export {};
